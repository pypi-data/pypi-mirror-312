[tool.poetry]
name = "type-fastapi"
version = "0.1.4"
description = ""
authors = ["KevinRawal <kevinrawal30@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
type-fastapi = "type_fastapi.main:app"

[tool.poetry.dependencies]
python = "^3.12"
typer = "^0.13.1"
pytest = "^8.3.3"


[tool.poetry.group.dev.dependencies]
pytest-cov = "^6.0.0"
black = "^24.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
