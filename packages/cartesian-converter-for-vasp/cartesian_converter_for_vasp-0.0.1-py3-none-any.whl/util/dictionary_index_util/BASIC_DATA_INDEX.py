from enum import Enum


class BasicDataIndex(Enum):
    NAME            = 'name'
    SCALING_FACTOR  = 'scaling_factor'
    LATTICE_VECTORS = 'lattice_vectors'
    ATOM_STAT       = 'atom_stat'
