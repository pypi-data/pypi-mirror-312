[tool.poetry]
name = "calculator-jms"
version = "0.1.0"
description = ""
authors = ["James Twose <james.twose2711@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
typer = "^0.14.0"

[tool.poetry.scripts]
calculator-jms = "calculator_jms.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
