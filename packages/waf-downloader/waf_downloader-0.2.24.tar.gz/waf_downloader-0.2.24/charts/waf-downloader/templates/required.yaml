# Defines required values
{{- $name := .Values.cloudflare.apiToken | required ".Values.cloudflare.apiToken is required." -}}
{{- $name := .Values.cloudflare.zoneIds  | required ".Values.cloudflare.zoneIds is required." -}}
