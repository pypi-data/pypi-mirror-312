[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
# Build artifacts with the specified Cargo profile
profile = "release"
bindings = "bin"
strip = true
manifest-path = "virialize/Cargo.toml"

