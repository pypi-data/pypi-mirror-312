[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "jing"
version = "0.0.6"
dependencies = [
    "requests",
    'importlib-metadata; python_version<"3.10"',
]
