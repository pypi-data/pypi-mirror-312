// const config = require('./config')
// const processArgv = require(config.pakPath + '/process.argv')

// ----------------------------------------------------------------------------------------------------
// cookies = headers = params = data = {}
// ----------------------------------------------------------------------------------------------------
const getCookies = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getHeaders = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getParams = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getData = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getResponseBody = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
// require.main === module && processArgv()