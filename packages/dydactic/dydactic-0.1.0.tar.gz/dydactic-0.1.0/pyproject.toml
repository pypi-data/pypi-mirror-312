[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dydactic"
version = "0.1.0"
description = "Validate iterables using Pydantic"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.9.2",
    "python-dateutil"
]