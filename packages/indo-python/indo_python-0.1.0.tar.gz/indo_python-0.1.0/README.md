# Indo-Python

Indo-Python overrides built-in Python functions with custom names:
- `print` becomes `bol`
- `len` becomes `duri`
- `input` becomes `le`

## Installation

```bash
pip install indo-python
