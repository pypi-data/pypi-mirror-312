[tool.poetry]
name = "poetry-drill-demo"
version = "0.1.0"
description = ""
authors = ["stevepurpose <stevenpurpose@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
poetry-drill-demo = "poetry_drill_demo.main:app"


[tool.poetry.dependencies]
python = "^3.8"
typer = "^0.14.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
