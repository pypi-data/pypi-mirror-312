[project]
name = "fastapi-forward"
version = "0.0.1"
description = "FastAPI request forwarding middleware"
authors = [
    {name = "Mkdir700", email = "mkdir700@gmail.com"},
]
dependencies = [
    "fastapi",
    "httpx>=0.28.0",
]
requires-python = "==3.10.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = true

[tool.pdm.dev-dependencies]
dev = [
    "pytest>=8.3.3",
    "uvicorn>=0.32.1",
]
