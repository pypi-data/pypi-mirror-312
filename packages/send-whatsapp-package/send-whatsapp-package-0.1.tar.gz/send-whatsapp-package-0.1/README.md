# send_whatsapp_package

A Python package to send WhatsApp messages instantly using `pyautogui` and `pywhatkit`.

## Installation

You can install the package with pip:

```bash
pip install send_whatsapp_package
