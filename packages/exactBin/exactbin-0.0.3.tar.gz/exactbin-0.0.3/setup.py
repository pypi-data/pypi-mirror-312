from setuptools import setup, find_packages

setup(
    name="exactBin",
    version="0.0.3",
    packages=find_packages(),
    description="A Python module for infinite precision binary floats and arithmetic.",
)