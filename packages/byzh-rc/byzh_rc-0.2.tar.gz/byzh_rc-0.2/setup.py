from setuptools import setup, find_packages

setup(
    name='byzh_rc',
    version='0.2',
    author="byzh_rc",
    description="更方便的深度学习",
    packages=find_packages(),
    install_requires=[
        # 依赖列表
    ],
)
