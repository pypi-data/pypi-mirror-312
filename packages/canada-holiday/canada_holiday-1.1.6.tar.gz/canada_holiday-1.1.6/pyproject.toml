[tool.poetry]
name = "canada_holiday"
version = "1.1.6"
description = "Python package for Canadian holidays"
authors = ["Sunmin Kim <kmsunmin@gmail.com>"]
readme = "README.md"
repository = "https://github.com/kmsunmin/canada-holiday"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
black = "^24.3.0"
pre-commit = "^3.2.0"
pytest = "^7.2.2"
pytest-cov = "^4.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
