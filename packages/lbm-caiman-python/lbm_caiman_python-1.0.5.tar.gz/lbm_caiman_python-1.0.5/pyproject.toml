[build-system]
requires = ["setuptools==68.2.2", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

#[tool.pixi.project]
#channels = ["conda-forge"]
#platforms = ["linux-64"]
#
#[tool.pixi.pypi-dependencies]
#LBM-CaImAn-Python = { path = ".", editable = true }
#
#[tool.pixi.tasks]
