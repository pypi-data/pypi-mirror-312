[tool.poetry]
name = "tian_repository"
version = "0.1.0"
description = "python.thienhang.com"
authors = ["open-thienhang-com"]
readme = "README.md"
packages = [{ include = "tian_repository" }]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
